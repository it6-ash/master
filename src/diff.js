#!/usr/bin/env node
// Stage 4. Snapshot comparison -> change events + auto-issue regeneration.
// Not implemented yet — see README "Build status".
console.error('npm run diff is not implemented yet (Stage 4).');
console.error('It needs at least two snapshots under data/snapshots/<server>/, which means ingest first.');
process.exit(2);
