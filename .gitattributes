# Authored on Windows, run on Linux. A CRLF in a shell script or a unit file
# fails on the far end in a way that reads like a missing binary.
*.sh       text eol=lf
*.service  text eol=lf
*.timer    text eol=lf
*.conf     text eol=lf
